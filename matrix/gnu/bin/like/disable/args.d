module args;

/**
 a function that is called when the function is called with the given arguments
 cat returns the function that is called with the given arguments.
*/

class args {
    public static void main (const char *name, const char *)  {
        // get the name of the function from the property list and return the function name
    }
}