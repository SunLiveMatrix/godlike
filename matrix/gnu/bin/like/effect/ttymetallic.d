module ttymetallic;

/**
  the default implementation of the interface for the TTY interface that is used by the TTY interface
  may be overridden by the TTY ineterfaces of the TTY interface that is used by the TTY interface that
  logically is used by the TTY interface that is use business.
*/

class TTYInterface {
    public static TTYInterfaceObjGetter(const char *name, const char *value) {
        // TODO: implementation specific validation of the TTY interface name and value
    }
}